{
  "name": "api-lambda-crud-dynamodb",
  "version": "1.0.0",
  "description": "Running an API Gateway with four Lambdas to do CRUD operations on DynamoDB",
  "private": true,
  "scripts": {
    "postinstall": "(cd lambdas && npm install);",
    "build": "tsc",
    "watch": "tsc -w",
    "cdk": "cdk",
    "deploy": "tsc && cdk deploy",
    "destroy": "tsc && cdk destroy"
  },
  "author": {
    "name": "Aleksandar Simovic <alexander.simovic@gmail.com>",
    "url": "https://serverless.pub"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "*",
    "aws-cdk": "*",
    "esbuild": "*",
    "typescript": "~3.9.7"
  },
  "dependencies": {
    "@aws-cdk/aws-synthetics-alpha": "^2.15.0-alpha.0",
    "aws-cdk-lib": "^2.0.0",
    "constructs": "^10.0.0"
  }
}
